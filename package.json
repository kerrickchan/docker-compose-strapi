{
  "dependencies": {
    "create-strapi-app": "4.11.5",
    "yarn": "^1.22.19"
  },
  "scripts": {
    "app": "yarn create strapi-app app --dbclient=postgres --dbhost=strapiDB --dbport=5432 --dbname=strapi --dbusername=strapi --dbpassword=strapi --dbssl=false --no-run --typescript"
  }
}
